// Owner-only strategy registry admin (FALCON_DISCOVER_SPEC §6). Gated by PilotShell like every member page; the owner check lives in the screen.
export {StrategyAdmin as default} from '../../src/admin/StrategyAdmin';

// Derivative tab (docs/DERIVATIVES_SPEC.md §4). Full-height member page under the shell header, like /discover.
import React from 'react';
import {DerivativeTab} from '../src/derivative';
export default function DerivativeRoute(){return <DerivativeTab/>;}

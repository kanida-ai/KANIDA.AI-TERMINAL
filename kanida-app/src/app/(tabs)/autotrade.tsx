import { StubScreen } from '@/components/StubScreen';

export default function AutoTradeScreen() {
  return (
    <StubScreen
      title="AutoTrade"
      body="Kanida never places an order on your behalf. When this surface is built it is a broker hand-off: you confirm every order inside your own broker. Nothing here yet, and nothing invented to fill the space."
    />
  );
}

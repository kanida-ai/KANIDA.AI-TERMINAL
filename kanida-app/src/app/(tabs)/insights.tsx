import { StubScreen } from '@/components/StubScreen';

export default function InsightsScreen() {
  return (
    <StubScreen
      title="Insights"
      body="The market base-rate feed lives here — every card carrying its n, its window and its cost convention. Not part of the Pathfinder slice."
    />
  );
}
